"""Character Engine Interface (Invariant 2 & 4: Character precedes plot, Advice != Command)."""
from typing import Protocol


class CharacterEngineProtocol(Protocol):
    """Abstract protocol for stable identity, motivation evaluation, and intent resolution."""
