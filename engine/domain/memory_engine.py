"""Memory & Knowledge Engine Interface (Invariant 6 & 8: Zero knowledge leak, domain memory separation)."""
from typing import Protocol


class MemoryKnowledgeEngineProtocol(Protocol):
    """Abstract protocol for character knowledge, belief states, and memory distillation."""
