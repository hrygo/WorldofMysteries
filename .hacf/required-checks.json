{
  "$comment": "受保护分支必需检查契约。检查名是公开接口：分支保护按『检查名』匹配，改名等同于让所有 PR 永久等待一个不会出现的检查（比失败更难排查）。修改本文件后必须同步 ruleset，并由 CI 断言契约成立。",
  "version": 1,
  "ruleset": "main-protection",
  "checks": [
    {
      "context": "All Quality Gates Passed",
      "workflow": ".github/workflows/ci.yml",
      "required": true,
      "level": "blocking",
      "description": "Stage 1-3 聚合门禁：架构适应度与契约、Python 引擎、Swift 6 客户端"
    },
    {
      "context": "Capsule Gate",
      "workflow": ".github/workflows/capsule-audit.yml",
      "required": true,
      "level": "blocking",
      "description": "任务胶囊边界与证据审计：仅 BLOCKING 级发现阻断合入，证据完备性默认降级为提示"
    }
  ]
}
