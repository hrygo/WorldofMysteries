import SwiftUI

/// 交互规范、排版标尺与通用原语：设计系统验证用画廊分组。
struct ComponentGalleryDesignSystemGroup: View {
    var body: some View {
        InteractionAndTypographyGallerySection()
        PrimitivesGallerySection()
        VisualSystemGallerySection()
    }
}
